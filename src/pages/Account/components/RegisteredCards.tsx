import React from "react";
import MyAccout from "..";

type Props = {};

const RegisteredCards = (props: Props) => {
  return (
    <MyAccout>
      <div id="cards" className="mt-28 p-7 h-screen bg-slate-400">RegisteredCards</div>
    </MyAccout>
  );
};

export default RegisteredCards;
