import React from "react";
import MyAccout from "..";

type Props = {};

const MemberInfo = (props: Props) => {
  return (
    <MyAccout>
      <div id="membership" className="mt-28 p-7 h-screen bg-yellow-400 ">MemberInfo</div>
    </MyAccout>
  );
};

export default MemberInfo;
